option_settings:
  aws:elasticbeanstalk:application:environment:
    UPLOAD_FOLDER: /var/app/current/uploads

commands:
  01_create_upload_folder:
    command: "mkdir -p /var/app/current/uploads && chmod -R 777 /var/app/current/uploads"
